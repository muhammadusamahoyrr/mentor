{
  "name": "healthcare-mcp",
  "version": "1.0.0",
  "description": "MCP server exposing the CareLoop clinical skills (web search, vector recall, patient files, appointments) as MCP tools, plus demo-only resources and prompts. Streamable HTTP for service-to-service; stdio for the Claude Code / Cursor demo.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "stdio": "node src/stdio.js",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --forceExit",
    "lint": "eslint ."
  },
  "author": "Muhammad Usama",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.6.0",
    "jest": "^30.4.2",
    "supertest": "^7.2.2"
  }
}
